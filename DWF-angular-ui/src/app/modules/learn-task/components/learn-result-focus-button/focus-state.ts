/** State outputed by {@link LearnResultFocusButtonComponent}. */
export interface FocusState {
  /** Whether the focus toggle is checked. */
  checked: boolean;
  /** Index of the selected option of the select. */
  index?: number;
}
